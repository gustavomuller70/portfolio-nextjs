services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      extra_hosts:
        - "host.docker.internal:host-gateway"
    ports:
      - "3000:80"
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
